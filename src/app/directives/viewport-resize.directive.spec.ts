import { ViewportResizeDirective } from './viewport-resize.directive';

describe('ViewportResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportResizeDirective();
    expect(directive).toBeTruthy();
  });
});
